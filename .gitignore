# Swift Package Manager
.build/
.swiftpm/
Package.resolved

# per `make app` erzeugtes .app-Bundle
dist/

# Xcode (falls das Package später in Xcode geöffnet wird)
*.xcodeproj
*.xcworkspace
xcuserdata/
DerivedData/
*.xcuserstate

# Separates Xcode-Scaffold-Projekt mit eigenem .git (nur lokal, u.a. für
# Icon-Validierung angelegt) — hat kein .gitmodules-Pendant, würde bei
# `git add -A` sonst versehentlich als kaputter Submodule-Gitlink landen.
/Music Assistant Menubar/

# macOS
.DS_Store

# Editor
.vscode/
.idea/
.claude/

# Lokale, nicht zu versionierende Notizen/Logs
*.log
